ALL: memcpy
SHELL = /bin/sh
DIRS = unaligned
OPT = -O
memcpy: memcpy.c
	@MPICC@ -o memcpy $(OPT) memcpy.c
clean:
	/bin/rm -f memcpy memcpy.o memcpy.log
	for dir in $(DIRS) ; do \
	    ( cd $$dir ; make clean ) ; done
